import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('title', (WidgetTester tester) async {
    // Test code goes here.
    //await tester.pumpWidget(Widget(title: 'T', message: 'M'));
    //expect(titleFinder, findsOneWidget);
  });
}
